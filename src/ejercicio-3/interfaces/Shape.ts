/**
 * Interfaz que representa una figura geométrica capaz de calcular su área.
 */
export interface Shape {
  /**
   * Calcula el área de la figura.
   * @returns Área de la figura en unidades cuadradas.
   */
  area(): number;
}
